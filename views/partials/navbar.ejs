<nav class="navbar navbar-expand-lg border-bottom border-secondary">
  <div class="container-fluid">
    <a class="navbar-brand d-flex align-items-center gap-2" href="/">
      <i class="bi bi-box-seam text-info"></i>
      <span class="fw-semibold">Inventory Control</span>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navMain">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navMain">
      <% if (typeof user !== 'undefined' && user) { %>
        <% if (user.is_leader) { %>
          <ul class="navbar-nav me-auto">
            <li class="nav-item">
              <a class="nav-link" href="/dashboard"><i class="bi bi-speedometer2"></i> Dashboard</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/inventory"><i class="bi bi-archive"></i> Inventory</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/groups"><i class="bi bi-collection"></i> Groups</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/users"><i class="bi bi-people"></i> Users</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/audit"><i class="bi bi-journal-text"></i> Audit Log</a>
            </li>
          </ul>
        <% } else { %>
          <ul class="navbar-nav me-auto">
            <li class="nav-item">
              <a class="nav-link" href="/my/dashboard"><i class="bi bi-speedometer2"></i> My Dashboard</a>
            </li>
          </ul>
        <% } %>
        <div class="navbar-nav">
          <span class="nav-link text-info"><i class="bi bi-person-circle"></i> <%= user.swc_name %></span>
          <form action="/auth/logout" method="POST" class="d-inline">
            <button type="submit" class="btn btn-outline-secondary btn-sm">Logout</button>
          </form>
        </div>
      <% } %>
    </div>
  </div>
</nav>
